import app from '../src/app';
import creditCard from '../src/api/routes/cardRoute';
import {expect} from 'chai';
import 'mocha';
/*
describe("Describe Test for the api", () => {
    it('Should retorn the status 200', async done => {
        const result = await app.routes(creditCard).get('/v1/CreditCard/?user_id=1');
        expect(result.request.statusCode).to.equal(200);
        done();
    });

});*/